# https://taskfile.dev

version: '3'

tasks:
  default:
    cmds:
      # Build
      - task: build
      # Run The Compiler
      - cd decaf/
      - dotnet run --project=decaf -- ./test.decaf
  test:
    cmds:
      # build
      - task: build
      # Run Test
      - dotnet test
  build:
    cmds:
      # Generate Lexer
      - antlr -Dlanguage=CSharp ./decaf/Lexing/DecafLexer.g4
      # Build the compiler
      - dotnet build
  format:
    cmds:
      # Csharp Formatter
      - dotnet format
  clean:
    cmds:
      # Clean Lexer
      - rm -rf ./decaf/Lexing/DecafLexer.cs
      - rm -rf ./decaf/Lexing/DecafLexer.tokens
      - rm -rf ./decaf/Lexing/DecafLexer.interp
